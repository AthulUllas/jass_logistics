/**
 * JASS Logistics - Frontend Configuration
 * Toggle API_BASE for local vs remote hosting.
 */
const CONFIG = {
    // Leave empty for same-domain hosting (Hostinger)
    // Use full URL (e.g. 'https://your-hostinger-domain.com/') for cross-domain (Vercel)
    API_BASE: 'https://logistics.unicules.com/',
};
